apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: user-service-monitor
  namespace: monitoring
  labels:
    app: user-service
    release: monitoring
spec:
  selector:
    matchLabels:
      app: user-service
  namespaceSelector:
    matchNames:
      - default    # Change to the namespace where your order-service is running
  endpoints:
    - port: http   # must match the Service's port name
      path: /metrics
      interval: 15s